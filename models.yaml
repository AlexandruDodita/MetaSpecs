roles:
  orchestrator:
    base_url: "https://opencode.ai/zen/go/v1"
    model: "deepseek-v4-flash"
    api_key: "${OPENCODE_API_KEY}"
  worker:
    base_url: "https://opencode.ai/zen/go/v1"
    model: "deepseek-v4-flash"
    api_key: "${OPENCODE_API_KEY}"
